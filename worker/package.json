{
  "name": "aired-transcode-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AIRED Phase 3 transcoding worker — turns one work's audio master into a streamable HLS rendition (AAC 192k, VOD) and stores it in Cloudflare R2.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/index.js",
    "transcode": "node src/cli.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/lib-storage": "^3.700.0",
    "@supabase/supabase-js": "^2.108.1",
    "ws": "^8.21.0"
  }
}
